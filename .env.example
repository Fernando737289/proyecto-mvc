# Configuración de base de datos
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root123
DB_NAME=backTarjetaVecino

# Seguridad (obligatorias: el backend no arranca sin ellas)
# Genera una clave con: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=cambia_esta_secret_key
# Genera una clave Fernet con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=jHGhqGEWJ-sSNHnQ_1B9y9_yOw9Jp8YUcdOfAPN2fBI=

# JWT
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Servicio externo DEC (validación de cédula)
API_URL=https://api-ejemplo.serviciodec.cl/validar
API_TOKEN=cambia_este_token

# CORS (JSON array)
# CORS_ORIGINS=["*"]

# Logging (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
